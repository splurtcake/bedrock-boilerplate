// Vendor
import whatInput from 'what-input';

/**
 * @class App
 * A basic class to hold instances of modules
 * Any other global config can be setup here
 */
class App {

  constructor() {
  }
}

// Init
const app = new App();

// Remove the no-js class
// document.documentElement.className = document.documentElementhtml.className.replace(/\bno-js\b/,'');